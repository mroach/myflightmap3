# Myflightmap

## Running with Docker

*First time:*

```shell
docker-compose build
docker-compose run web mix do ecto.create, ecto.migrate
```

*Start dev server:*

```shell
docker-compose run web
```
