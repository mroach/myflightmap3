<h2 class="title is-2">Edit Flight</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.flight_path(@conn, :update, @flight)) %>

<span><%= link "Back", to: Routes.flight_path(@conn, :index) %></span>
