<h2 class="title is-2">New Airport</h2>

<%= render "form.html", Map.put(assigns, :action, Routes.airport_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.airport_path(@conn, :index) %></span>
